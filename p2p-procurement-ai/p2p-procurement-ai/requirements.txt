# Local development dependencies
# Databricks runtime dependencies (pyspark, dlt, mlflow) are
# pre-installed in the Databricks cluster environment

# Dataset generation
reportlab>=4.0.0
faker>=20.0.0

# Azure Blob Storage
azure-storage-blob>=12.19.0

# Upload utility
tqdm>=4.66.0

# Eval harness (local use)
scikit-learn>=1.3.0
pandas>=2.0.0
tabulate>=0.9.0
mlflow>=2.9.0

# PDF text extraction (also installed in Databricks via %pip)
pdfminer.six>=20221105
pypdf>=3.0.0
